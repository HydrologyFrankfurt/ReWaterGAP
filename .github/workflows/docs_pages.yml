name: doc_pages_workflow
on: push


jobs:

  build_docs_job:
    name: Build docs
    runs-on: ubuntu-latest  
    steps:
      - name: Checkout code  
        uses: actions/checkout@v2
  
      - name: Set up Python
        uses: actions/checkout@v2
        with: 
          python-version: 3.9
  
      - name: Install dependencies 
        run: |
          python -m pip install -U sphinx
          python -m pip install sphinx-rtd-theme
        
      - name: make sphinx docs
        run: |
          make -c docs clean
          make -c docs html
      
      - name: add .nojekyll file in a new folder docs/build/html
        run: |
          cd docs/build/html/
          git init 
          touch .nojekyll
          git add -A
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git commit -m deploy
          

          
      
      
      
      
